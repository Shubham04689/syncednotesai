# Web framework
fastapi>=0.115.0
uvicorn>=0.30.0

# Data validation
pydantic>=2.7.0

# Environment & config
python-dotenv>=1.0.1

# File upload support
python-multipart>=0.0.9

# PDF parsing & rendering
pymupdf>=1.24.0

# Async HTTP client (used as fallback for all API calls)
httpx>=0.27.0

# WebSocket support for FastAPI
websockets>=13.0

# ── AI Provider SDKs (latest stable as of 2025-06) ────────────────────────────

# Google Gemini — new unified Gen AI SDK (GA since May 2025)
# Replaces deprecated google-generativeai
google-genai>=1.15.0

# Groq — official async Python SDK (v1.x)
groq>=1.2.0

# Mistral AI — official Python SDK (v2.x)
mistralai>=2.4.4

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
